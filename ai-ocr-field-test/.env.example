GEMINI_API_KEY=
OPENAI_API_KEY=
MISTRAL_API_KEY=

VLM_PROVIDER=gemini
VLM_MODEL=

# Unlimited-OCR (Baidu, GPU-only). Run the model on a GPU (see
# colab/unlimited_ocr_colab.ipynb), tunnel its port, and paste the URL here.
# Leave blank to skip this runner (it fails gracefully).
UNLIMITED_OCR_BASE_URL=
UNLIMITED_OCR_API_KEY=EMPTY
UNLIMITED_OCR_MODEL=Unlimited-OCR
UNLIMITED_OCR_PROMPT=document parsing.
