# Screenshots

Put screenshots here for the public write-up (LinkedIn post / carousel / README).

Suggested captures:

- A clean invoice extracted correctly (the "easy" baseline).
- A messy/blurry scan where OCR struggles.
- A table-heavy document showing how structure is preserved (or lost).
- A hallucination example (VLM inventing a value that isn't in the document).
- The final ranking from `results/final_report.md`.

Reference these files from `results/final_report.md` (Worst Failure section) and `post/`.
